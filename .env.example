# Configuration du calendrier Google
CALENDAR_ID=happy.rouret@gmail.com
TIMEZONE=Europe/Paris
DAYS_AHEAD=14

# Configuration de l'email
# Templates disponibles: design_moderne, design_classique, design_festif, design_minimaliste
EMAIL_TEMPLATE=design_classique
EMAIL_SUBJECT=Happy au Rouret - Prochains événements
FROM_NAME=Happy au Rouret

# Configuration SMTP
# ⚠️ IMPORTANT : Les identifiants (login/password) doivent être dans ~/.netrc, PAS ici !
# Voir .netrc.example et QUICKSTART.md pour la configuration

# Gmail (par défaut)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true

# Autres fournisseurs :
# OVH
# SMTP_HOST=ssl0.ovh.net
# SMTP_PORT=587
# SMTP_USE_TLS=true

# Office 365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USE_TLS=true

# ⚠️ NE PAS mettre SMTP_USER et SMTP_PASSWORD ici !
# Ces informations doivent être dans ~/.netrc pour des raisons de sécurité.
# Exemple de contenu pour ~/.netrc :
#   machine smtp.gmail.com
#     login votre.email@gmail.com
#     password votre_mot_de_passe_application
# N'oubliez pas : chmod 600 ~/.netrc

# Destinataires (séparés par des virgules)
TO_ADDRESSES=email1@example.com,email2@example.com
